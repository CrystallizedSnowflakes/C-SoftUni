using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;

namespace _19.PokemonDon_tGo
{
    class PokemonDonnottGo
    {
        static void Main(string[] args)
        {
            //int[] distancesToThePokemons = Console.ReadLine().Split().Select(int.Parse).ToArray();
            //List<int> arr = new List<int>();
            //var sum = 0;

            //while (distancesToThePokemons.Length > 0)
            //{
            //    int nextIndex = int.Parse(Console.ReadLine());

            //    foreach (var num in distancesToThePokemons)
            //    {
            //        if (nextIndex < 0)
            //        {
            //            arr[0] = distancesToThePokemons.Last();
            //        }
            //        else if (nextIndex > distancesToThePokemons.Length)
            //        {
            //            arr[arr.Count - 1] = distancesToThePokemons[0];
            //        }
            //        else if (nextIndex < arr.Count() && nextIndex >= 0)
            //        {
            //            if (arr[num] == distancesToThePokemons[nextIndex])
            //            {
            //                sum += distancesToThePokemons[nextIndex];
            //                arr.RemoveAt(num);
            //            }
            //            else if (num < nextIndex)
            //            {
            //                arr[num] += distancesToThePokemons[nextIndex];
            //            }
            //            else
            //            {
            //                arr[num] -= distancesToThePokemons[nextIndex];
            //            }
            //        }
            //    }
            //}         
            //Console.WriteLine(sum);
        }
    }
}
